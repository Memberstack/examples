import { useState, useEffect } from "react";
import withLayout from "@/app/layouts/withLayout";

const Signup = () => {
  return <div className="relative h-full mx-auto">signup</div>;
};

export default withLayout(Signup, "basic");
